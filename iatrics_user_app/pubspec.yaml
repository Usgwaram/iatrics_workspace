name: iatrics_user_app
description: Iatrics Telemedicine User App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  flutter_secure_storage: ^10.0.0
  provider: ^6.0.3
  image_picker: ^1.0.0
  camera: ^0.12.0
  flutterwave_standard: ^1.0.3
  shared_preferences: ^2.2.2
  multicast_dns: ^0.3.2
  url_launcher: ^6.2.1
  socket_io_client: ^3.1.4
  agora_rtc_engine: ^6.5.3
  permission_handler: ^12.0.1
  audioplayers: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/audio/ringtone.mp3